STELLAR CARTOGRAPHICA v26.0 TEST REPORT
Build: 26.0-combat-claims-save-fleet-lifecycle-v44-tested

PASS — all embedded JavaScript blocks and service worker pass syntax validation.
PASS — manifest validates as Revision 26.0.
PASS — all literal button IDs and button data-action families have matching interaction paths after the shared map-control framework is accounted for.

CAMPAIGN SAVE EXPORT
PASS — campaign JSON serialization path validated.
PASS — mocked browser fallback successfully generated a JSON download click.
UPDATED — iOS/PWA export prefers the native share/save sheet with a real JSON File; normal download and new-tab fallback remain available.

FLEET COMBAT / LIFECYCLE
PASS — direct fleet target discovery exposes foreign fleets co-located in the same system.
PASS — direct fleet engagement uses a stationary/co-located aftermath rather than invasion breakthrough movement.
PASS — repair quote and repair state transition tested on a 40%-damaged Expeditionary Fleet.
PASS — 0% Hull destruction removes the fleet and its embarked Task Forces.
PASS — Tier-0 Expeditionary Vessel seeding tested for both player and AI factions.

GROUND COMBAT
PASS — a deployed army discovered a hostile target in another territory on the same world.
PASS — declaring the cross-territory battle consumed 1 Supply + 1 Command and created a pending planetary territory battle.
UPDATED — Overview and planetary ground-operation panels expose direct ground-battle controls.

TERRITORY CLAIMS / PARTIAL WORLDS
UPDATED — planetary map and Territory-tab modal claims use one delegated claim-and-refresh path.
UPDATED — partially claimed planets display territory-by-territory ownership.
UPDATED — settlement founding checks the selected territory, so an unrelated foreign claim elsewhere on the same planet no longer blocks an unclaimed/friendly region.
PASS — v25.9 exact-territory fleet disembarkation remains integrated.

DEEP TIME
PASS — the independent v25.7 stack-safe Deep-Time engine remains integrated.

LIMITATION
The container Chromium environment is not reliable for local file/localhost click-through. Runtime-critical state transitions were therefore tested directly with the exact v26.0 functions in Node harnesses, plus a complete static interaction audit.
