Administratum Stellar Cartographica — Revision 24.7
Build: 24.7-deep-time-battlefleet-v28

Validation completed:
- All 6 inline JavaScript blocks extracted from index.html and passed `node --check`.
- sw.js passed `node --check`.
- manifest.webmanifest parsed successfully as JSON.
- Required Revision 24.7 feature markers verified in index.html:
  campaign chronology controls, 100–1000 turn slider, naval battle system,
  fleet auto-resolve controls, strategic non-adjacent targeting, and build identity.
- Static build metadata (HTML bootstrap, manifest, service worker, VERSION) updated to v24.7.
- Release archive tested with ZIP integrity check after packaging.

Browser-runner note:
- A local headless Chromium `--dump-dom` smoke test was attempted. Chromium loaded but did not terminate within the bounded test window in this container, matching the behaviour observed with the prior self-contained build. Because the runner did not return a clean completion signal, this is not recorded as a passed interactive browser regression test.

Design/reference note:
- The naval layer is an original strategic abstraction inspired by the user-provided Battlefleet Gothic Remastered rulebook and fleet book. It does not embed or reproduce source PDFs, artwork, full fleet-list text, or exact ship profiles.
